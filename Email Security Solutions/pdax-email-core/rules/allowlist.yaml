# SEGS Allowlist — trusted senders / domains that bypass scoring and are always delivered.
# Addresses take precedence over domain entries.
# Changes here take effect on the next email processed (no restart needed).
entries: []
# Example entries:
#   - address: noreply@pdax.ph
#     note: "PDAX internal system mail"
#   - domain: paypal.com
#     note: "Known trusted vendor"
